---
# Add required tools for handling keys and repositories
- name: Install gnupg
  ansible.builtin.apt:
    name: gnupg
    state: present

# Add Pritunl GPG key and repository
- name: Add Pritunl GPG key
  ansible.builtin.get_url:
    url: "https://raw.githubusercontent.com/pritunl/pgp/master/pritunl_repo_pub.asc"
    dest: /usr/share/keyrings/pritunl.gpg
    mode: '0644'

- name: Add Pritunl repository
  ansible.builtin.apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/pritunl.gpg] https://repo.pritunl.com/stable/apt noble main"
    state: present

# Add MongoDB GPG key and repository
- name: Add MongoDB GPG key
  ansible.builtin.get_url:
    url: "https://www.mongodb.org/static/pgp/server-6.0.asc"
    dest: /usr/share/keyrings/mongodb.gpg
    mode: '0644'

- name: Add MongoDB repository
  ansible.builtin.apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/mongodb.gpg] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/6.0 multiverse"
    state: present

# Update package cache
- name: Update package cache
  ansible.builtin.apt:
    update_cache: yes

# Install required packages
- name: Install Pritunl, MongoDB, and WireGuard
  ansible.builtin.apt:
    name:
      - pritunl
      - mongodb-org
      - wireguard
      - wireguard-tools
    state: present

# Disable UFW
- name: Disable UFW
  ansible.builtin.ufw:
    state: disabled

# Enable and start services
- name: Enable and start MongoDB and Pritunl
  ansible.builtin.systemd:
    name: "{{ item }}"
    enabled: true
    state: started
  loop:
    - mongod
    - pritunl
